import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    borderWidth: 0,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 10,
    flex: 1,
  },
  buttonText: { textAlign: 'center', fontSize: 48 },
});
